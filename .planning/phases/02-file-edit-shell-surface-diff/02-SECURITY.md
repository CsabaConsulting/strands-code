# Phase 2 Security Audit — File/Edit/Shell Surface + /diff (TOOL-01/02/04)

Auditor: gsd-security-auditor. Code as of this audit; no code changed (report only).
Focused suite: `tests/test_tool_surface.py` + `tests/test_diff_gate.py` + `tests/test_search.py` — **70 passed** (this session).

## Verdicts

| Threat | Disposition | Verdict | Evidence |
|--------|-------------|---------|----------|
| T-02-01 symlink/canonicalize confine (high) | mitigate (BLOCKING) | VERIFIED | [scope.py](/home/csaba/repos/AWS/strands-code/strands_code_cli/scope.py:35) `confine` uses `os.path.realpath` on both candidate and roots, admits only cwd+subdirs and /tmp, raises `ValueError` naming allowed roots (:52). Enforced at every mutation entry: [diff_gate.py](/home/csaba/repos/AWS/strands-code/strands_code_cli/diff_gate.py:245) and gated `edit` (:388); search path too ([search_tool.py](/home/csaba/repos/AWS/strands-code/strands_code_agent/search_tool.py:43)). Negative tests: sibling refusal, `..` traversal, symlink escape ([test_tool_surface.py](/home/csaba/repos/AWS/strands-code/tests/test_tool_surface.py:91)), plus gate-level (`test_scope_refusal_in_gate`) and search-level (`test_path_out_of_scope_refused`) refusals. |
| T-02-02 stale preview TOCTOU (high) | mitigate (BLOCKING) | VERIFIED | `_gate_and_apply` re-reads before apply and raises instead of misapplying ([diff_gate.py](/home/csaba/repos/AWS/strands-code/strands_code_cli/diff_gate.py:266)); `apply_stashed` re-reads per file, keeps stale entries pending, pops only on success (:301). Tests: `test_stale_file_exact_once_failure` (nothing written) and `test_stale_stash_kept_pending` ([test_diff_gate.py](/home/csaba/repos/AWS/strands-code/tests/test_diff_gate.py:230)). |
| T-02-03 shell escape (medium) | accept+document | VERIFIED (accepted, advisory present) | Advisory in gate docstring ([diff_gate.py](/home/csaba/repos/AWS/strands-code/strands_code_cli/diff_gate.py:17)), `scope.py` docstring (:8), agent prompt steering, and plan summary (shell/python_repl bypass as advisory; airtight mediation = Phase 3). Gate-docstring test pins it (`test_gate_docstring_documents_shell_bypass`). |
| T-02-04 python_repl `open()` writes (medium) | accept+document | VERIFIED (accepted, advisory present) | Same advisory chain as T-02-03; prompt explicitly steers away from `open()` (code_agent.py instructions) with a pinning test (`test_prompt_steers_away_from_python_open`). Enforcement deferred to Phase 3 as dispositioned. |
| T-02-05 rg pattern injection (medium) | mitigate (BLOCKING) | VERIFIED | `subprocess.run(argv, ...)` with no `shell=True`, pattern as a single argv element, 30 s timeout ([search_tool.py](/home/csaba/repos/AWS/strands-code/strands_code_agent/search_tool.py:49)); stdlib fallback is shell-free; `--glob` is likewise an argv element. Tests force the fallback via `shutil.which → None` and cover empty/invalid patterns. |
| T-02-06 diff preview disclosure (low) | accept | VERIFIED (accepted as dispositioned) | Preview is computed from content the gate just read in-turn (`old_text`/`current`), so it renders nothing the agent did not already hold. No new exfiltration surface introduced. |
| T-02-07 DoS: huge rg / long shell (low) | mitigate (rg) + accept+document (shell) | VERIFIED with accepted residual — rg half verified, shell half accepted | rg: result cap + truncation note ([search_tool.py](/home/csaba/repos/AWS/strands-code/strands_code_agent/search_tool.py:90)) with test (`... truncated to 5 of 20 matches.`), plus the 30 s rg timeout (:55). Shell: harness `ShellConfig` exposes only `description` (no timeout knob — `strands_harness/types/agent.py:73-80`), so the 120 s SDK default cannot be pinned from repo code; inherited upstream bound accepted and carried as residual risk #2 below. |
| T-02-08 wrapper name collision (medium) | mitigate (BLOCKING) | VERIFIED | `builtin_tools` mapping pins `write`/`edit` off, `shell`/`read` on ([main.py](/home/csaba/repos/AWS/strands-code/strands_code_cli/main.py:97)); wrappers carry the same names ([diff_gate.py](/home/csaba/repos/AWS/strands-code/strands_code_cli/diff_gate.py:337)); tests assert single registration (`test_wrapper_name_collision`, `test_gated_wrappers_replace_builtins`) and mapping-is-dict (future defaults still flow). |

## Residual risk carried to Phase 3

1. **Shell + python_repl file-mutation bypass (T-02-03/T-02-04):** accepted by design; airtight mediation of all tools is Phase 3 (TOOL-03) work. Prompt steering is the only current control.
2. **Shell long-run timeout (T-02-07 shell half):** confirm/pin the harness shell timeout in Phase 3 or record the harness default as verified upstream behavior; currently assumed, not evidenced in this repo.
3. **Minor:** `pending.json` sidecar persists file contents on disk (by design, session dir is `chmod 700` in `main.py:85`); T-02-06-adjacent — note for Phase 3 review of pending-state sensitivity, not a new finding.
