---
phase: 01-session-wiring-repl-skeleton
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - strands_code_cli/main.py
  - strands_code_cli/provider_config.py
  - strands_code_cli/first_run.py
  - tests/test_kill_resume.py
  - tests/test_first_run.py
autonomous: true
requirements: [SES-01, SES-03]
estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - First run without AWS credentials stops with a Bedrock setup pointer and creates nothing (per D-05 SES-03)
    - Provider choice persists in a config file across runs for Phase 5 to inherit (per D-06)
    - Clean exit (Ctrl-D, /exit, SIGTERM) explicitly saves and flushes session state (per D-08 SES-03)
    - SIGKILL mid-idle resumes with transcript plus pending tool state and working plan intact (per D-07 SES-03)
  artifacts:
    - strands_code_cli/provider_config.py
    - strands_code_cli/first_run.py
    - tests/test_kill_resume.py
    - tests/test_first_run.py
  key_links:
    - main.py runs the first_run preflight BEFORE create_harness construction
    - provider_config model string feeds create_harness(model=...) as a constructor kwarg
    - clean-exit path calls explicit save plus index update; kill-safety rests on per-message saves, never atexit
  prohibitions:
    - MUST NOT fall back to a local provider on first run without credentials; stop with the Bedrock pointer (D-05)
    - MUST NOT depend on atexit for session durability; it does not run on SIGKILL
    - MUST NOT log or persist secrets from the provider config or credential probe
  assumptions:
    - FLAGGED ASSUMPTION (unresolved LOOP-01 empty-input probe): empty input submits nothing and the loop re-prompts rather than sending an empty turn
    - FLAGGED ASSUMPTION (unresolved SES-01 probe): resuming an unknown or malformed session id surfaces a usage error and starts no session
    - FLAGGED ASSUMPTION (unresolved SES-03 probe): durability is per completed message; an in-flight turn interrupted by SIGKILL is not replayed on resume
---

<objective>
Durability slice: first-run Bedrock-or-stop gate, provider config persistence, and the kill-resume guarantee.

Purpose: Lock the D-05/D-06/D-07/D-08 decisions so resume never silently loses work and Phase 5 inherits a persisted provider.
Output: `provider_config.py`, `first_run.py`, gate wiring in `main.py`, kill-resume and first-run tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-session-wiring-repl-skeleton/01-CONTEXT.md
@.planning/phases/01-session-wiring-repl-skeleton/01-RESEARCH.md
@.planning/phases/01-session-wiring-repl-skeleton/01-PATTERNS.md
@.planning/phases/01-session-wiring-repl-skeleton/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task: provider config plus first-run gate</name>
  <files>strands_code_cli/provider_config.py, strands_code_cli/first_run.py, strands_code_cli/main.py</files>
  <read_first>strands_code_agent/knowledge/bundle.py; strands_code_agent/toolkits.py; strands_code_agent/python_environments/agentcore.py; strands_code_cli/main.py</read_first>
  <action>Per PATTERNS provider_config analog implement a ProviderConfig dataclass with yaml safe_load and safe_dump under the platformdirs user config dir, schema-validating keys and rejecting unknowns per ASVS V14, feeding the loaded model string into create_harness as a constructor kwarg per D-06; per PATTERNS first_run analog probe the ambient boto3 chain with a cheap STS get_caller_identity call before any construction per D-05, on failure print the Bedrock setup pointer and exit non-zero creating neither session dir nor config, never reading credentials directly; wire the gate in main.py strictly before session resolution and construction per RESEARCH Pitfall 5; note the costly reversibility of D-05 in the gate code path choice.</action>
  <verify>
    <automated>uv run pytest tests/test_first_run.py -x -q</automated>
    <fails_when>exit code != 0 or output contains FAILED or ERROR</fails_when>
  </verify>
  <done>Gate runs before construction; no-creds exit is non-zero with pointer and zero side effects; config round-trips.</done>
  <acceptance_criteria>No-creds run exits non-zero, prints the Bedrock pointer, and creates no session dir or config file; provider choice persists across runs.</acceptance_criteria>
  <reversibility rating="costly">D-05 Bedrock-or-stop tightens the AWS-optional constraint; an offline-first entry later reworks launch and session defaults.</reversibility>
</task>

<task type="auto">
  <name>Task: kill-resume plus exit-flush tests</name>
  <files>tests/test_kill_resume.py</files>
  <read_first>strands_code_agent/python_environments/local_sandboxed.py; tests/test_code_agent.py</read_first>
  <action>Per RESEARCH Pitfall 2 define full state as transcript plus agent state as of the last completed message, asserting continuity and never in-flight tool replay; write a SIGKILL-simulation test restoring without any exit flush per D-08 proving per-message saves suffice, plus a clean-exit test asserting explicit save plus index update per SES-03; mirror the salvage-not-raise ethos from the local_sandboxed analog.</action>
  <verify>
    <automated>uv run pytest tests/test_kill_resume.py -x -q</automated>
    <fails_when>exit code != 0 or output contains FAILED or ERROR</fails_when>
  </verify>
  <done>Restore-without-flush keeps transcript and state; clean exit flush is deterministic.</done>
  <acceptance_criteria>Kill-resume test passes with no exit flush in the path; clean-exit test proves the explicit save ran; neither test requires live credentials.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task: first-run negative tests</name>
  <files>tests/test_first_run.py</files>
  <read_first>strands_code_agent/document_code.py; strands_code_cli/first_run.py</read_first>
  <action>Per PATTERNS first_run test analog write fail-fast contract tests asserting the no-creds path exits non-zero, prints the setup pointer, and creates no session dir, following the TypeError and ValueError contract-test style of the document_code analog; keep the gate testable with mocked credential probe so CI stays offline-clean.</action>
  <verify>
    <automated>uv run pytest tests/test_first_run.py tests/test_kill_resume.py -x -q</automated>
    <fails_when>exit code != 0 or output contains FAILED, ERROR, or SKIPPED</fails_when>
  </verify>
  <done>First-run negative behavior is pinned without AWS mocks leaking into other tests.</done>
  <acceptance_criteria>Both files pass offline; no-creds run leaves the filesystem untouched.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| credential probe→boto3 chain | Ambient credentials are touched read-only, never handled in code |
| config file→CLI | User-editable YAML flows into model construction |
| exit path→snapshot store | Final flush writes whole-agent state to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information disclosure | provider_config.py / first_run.py | high | mitigate | Never log credential material or full config contents; config file holds only the provider string; probe errors surface a static pointer. BLOCKING until verified by code inspection plus the negative test. |
| T-03-02 | Tampering | provider config file | medium | mitigate | Schema-validate keys, reject unknowns, fail-soft to defaults on corrupt YAML; unknown keys never reach create_harness. |
| T-03-03 | Denial of service | exit-flush path | low | accept | Flush is best-effort on top of per-message saves; a failed flush on clean exit still leaves per-message state intact. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` full suite green including the 177-test baseline.
- `uv run pytest tests/test_first_run.py tests/test_kill_resume.py tests/test_session_resume.py -x -q` green offline.
- Manual: SIGKILL the CLI mid-idle, relaunch with the same id, confirm full transcript plus working plan.
- Planner scans: API-coverage — no external REST API (STS probe rides the ambient boto3 chain, no new HTTP contract); assumption-delta — no second-case transition; schema-gate — no ORM files (YAML config and JSON index need no migration).
</verification>

<success_criteria>
- No-creds first run stops with pointer, non-zero exit, zero side effects.
- Kill-resume restores transcript plus agent state; clean exit flushes deterministically.
- Provider choice persists for Phase 5 `/model` to inherit.
</success_criteria>

<output>
Create `.planning/phases/01-session-wiring-repl-skeleton/01-03-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

New symbols: `strands_code_cli.provider_config.ProviderConfig` (`load`, `save`), `strands_code_cli.first_run.preflight_credentials`.
New flags: none (wires plan 01 `--session-id`; first-run gate takes no new flags).
New files: `strands_code_cli/provider_config.py`, `strands_code_cli/first_run.py`, `tests/test_kill_resume.py`, `tests/test_first_run.py`.
